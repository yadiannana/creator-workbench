# 宝妈创作工作台 - 部署指南

## 🎯 这是什么

一个手机端单文件网页工作台，帮你每天搞自媒体创作。**不需要服务器、不需要数据库**，数据存在手机浏览器里。

---

## 📋 部署步骤（共5步）

### 第1步：注册 GitHub 账号（已有跳过）

1. 打开 https://github.com 点击 Sign up
2. 用邮箱注册，记住用户名和密码

### 第2步：创建仓库

1. 登录后点右上角 **+** → **New repository**
2. 仓库名填：`creator-workbench`
3. 选 **Public**（公开）
4. 勾选 **Add a README file**
5. 点 **Create repository**

### 第3步：上传网页文件

把以下文件上传到仓库根目录：
- `index.html`（主文件）

**上传方法：**
1. 在仓库页面点 **Add file** → **Upload files**
2. 把 `index.html` 拖进去
3. 点 **Commit changes**

同时上传 `.github/workflows/` 文件夹下的两个yml文件和 `scripts/update_content.py`。

> 💡 更简单的方法：点 **Add file** → **Create new file**，把文件内容粘贴进去。

### 第4步：开启 GitHub Pages

1. 在仓库页面点 **Settings** → 左侧 **Pages**
2. **Source** 选 **GitHub Actions**
3. 保存

此时会自动触发部署，1-2分钟后你的工作台就能访问了：
```
https://你的用户名.github.io/creator-workbench/
```

手机打开这个网址，可以**添加到桌面**当App用：
- iPhone：Safari打开 → 分享 → 添加到主屏幕
- 安卓：Chrome打开 → 菜单 → 添加到主屏幕

### 第5步：创建 Gist（自动更新用）

1. 打开 https://gist.github.com 登录
2. 文件名填：`creator-data.json`
3. 内容随便填点：`{}`
4. 点 **Create public gist**
5. 创建后，看浏览器地址栏，URL类似：
   ```
   https://gist.github.com/你的用户名/abc123def456
   ```
   最后一串字符就是 **Gist ID**，复制下来

### 第6步：配置自动更新

回到你的仓库 `creator-workbench`：

1. **Settings** → **Secrets and variables** → **Actions**
2. 点 **New repository secret**，添加：
   - Name: `GIST_ID`
   - Value: 第5步复制的Gist ID

3. （可选）如果要AI改写，添加：
   - `AI_API_KEY`：你的AI API密钥
   - `AI_API_URL`：API地址（默认OpenAI格式）

4. （可选）如果要抓微信专辑：
   - `WECHAT_ALBUM_IDS`：专辑ID，多个用逗号分隔

### 第7步：更新 index.html 中的 Gist ID

打开 `index.html`，找到这行：
```javascript
GIST_ID: '',  // ← 这里填你的公开Gist ID
```
改成：
```javascript
GIST_ID: '你的Gist ID',
```
提交更改，自动重新部署。

---

## ✅ 验收清单

- [ ] 手机能打开 `https://用户名.github.io/creator-workbench/`
- [ ] 左上角菜单能打开抽屉导航
- [ ] 八个模块都能切换
- [ ] 每日计划能勾选、进度条能动
- [ ] 选题灵感能跳转抖音/B站/小红书/夸克
- [ ] 热点二创能复制文案
- [ ] 内容复盘能保存
- [ ] 备忘录能加行
- [ ] 速算练习能答题
- [ ] 英语练习能朗读（点击🔊按钮）
- [ ] 猜字谜能玩
- [ ] GitHub Actions 定时任务能跑（手动触发测试）

---

## 🕐 自动更新说明

GitHub Actions 会在每天**早8点、下午2点、晚8点**自动运行：
1. 抓取抖音/微博/B站/百度/知乎热榜
2. 用AI改写成贴合"宝妈勇闯自媒体"的内容
3. 推送到你的Gist
4. 网页点刷新按钮即可拉到最新内容

手动触发测试：仓库 → Actions → 选"每日自动更新" → Run workflow

---

## ❓ 常见问题

**Q: 网页打不开？**
A: 检查 Settings → Pages 是否开启，Actions 是否跑成功。

**Q: 刷新后内容没变？**
A: 检查 Gist ID 是否填对，Actions 是否跑成功。

**Q: 数据会丢吗？**
A: 不会。数据存在浏览器本地，只要不清缓存就在。建议每周截图备份关键数据。

**Q: 换手机了怎么办？**
A: 新手机打开网址，数据是新的一套。复盘历史需要重新记录。

**Q: 抖音B站按钮点了没反应？**
A: 手机上会尝试唤起App，如果没装会跳网页版。
